# The SHIPPED tier, as one declaration. audit_harness.py fails if measure.sh, eval40.sh or the docs disagree.
# When a tier recipe changes, update THIS file in the same commit (doc-drift trigger).
VAE_FILE=vae-encoder-convint8.gguf
LM_FILE=lm-q8head.gguf
PIECES=1
